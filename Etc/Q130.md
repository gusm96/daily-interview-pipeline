<!-- meta id=Q130 slug=Etc date=2026-07-21 answered=false ai_auto=true -->
# [Q130] MSA에서 서비스 디스커버리 패턴의 필요성

> 🧩 기타 (Python·FastAPI·Next.js / MSA·CI·CD·대용량·테스트) · 2026-07-21

**Q.** 마이크로서비스 아키텍처(MSA) 환경에서 서비스 디스커버리(Service Discovery) 패턴이 왜 필요한지 설명하고, 클라이언트 측 디스커버리와 서버 측 디스커버리 방식의 차이점과 각각의 장단점, 그리고 어떤 상황에서 어떤 방식을 선택하는 것이 유리한지 설명해 주세요.

## 🧑‍💻 나의 답변

[⚠️ AI 자동 작성 답변 - 미응시]
## 마이크로서비스 아키텍처(MSA) 환경에서 서비스 디스커버리(Service Discovery) 패턴

### 1. 서비스 디스커버리 패턴의 필요성

MSA 환경에서는 수많은 서비스 인스턴스가 동적으로 생성, 삭제되고 IP 주소와 포트 번호가 변경될 수 있습니다. 서비스 디스커버리 패턴은 이러한 동적인 환경에서 클라이언트가 특정 서비스의 가용 인스턴스를 찾아 통신할 수 있도록 돕는 핵심 메커니즘입니다. 이를 통해 서비스 간의 느슨한 결합을 유지하고, 시스템의 유연성, 확장성, 가용성을 확보할 수 있습니다.

### 2. 클라이언트 측 디스커버리(Client-Side Discovery)

*   **핵심 정의:** 클라이언트가 직접 서비스 레지스트리에서 서비스 인스턴스의 네트워크 위치를 조회한 후, 해당 인스턴스로 요청을 전송하는 방식입니다.
*   **동작 원리:**
    *   서비스 인스턴스는 시작 시 자신의 네트워크 위치(IP, Port)를 서비스 레지스트리에 등록합니다.
    *   클라이언트는 서비스 호출 시 서비스 레지스트리에 질의하여 대상 서비스의 가용 인스턴스 목록을 가져옵니다.
    *   클라이언트는 로드 밸런싱 알고리즘(예: 라운드 로빈)을 사용하여 인스턴스 중 하나를 선택하고 직접 요청을 보냅니다.
*   **장점:**
    *   중앙 집중식 라우터/로드 밸런서가 필요 없어 네트워크 홉(hop)이 줄어듭니다.
    *   클라이언트가 직접 로드 밸런싱 로직을 제어할 수 있어 유연성이 높습니다.
    *   서비스 레지스트리 외에 추가적인 인프라 구성이 간단합니다.
*   **단점:**
    *   모든 클라이언트에 서비스 디스커버리 로직(레지스트리 조회, 로드 밸런싱)을 구현해야 합니다.
    *   다양한 언어로 구현된 클라이언트가 있을 경우, 각 언어별로 라이브러리를 개발하거나 통합해야 하는 복잡성이 있습니다.
    *   클라이언트 로직 변경 시 모든 클라이언트를 업데이트해야 할 수 있습니다.
*   **적합한 상황:**
    *   클라이언트의 수가 비교적 적고, 클라이언트 기술 스택이 통일되어 있는 경우.
    *   클라이언트가 직접 로드 밸런싱 전략을 세밀하게 제어해야 하는 경우.
    *   인프라 복잡성을 최소화하고 싶은 경우.

### 3. 서버 측 디스커버리(Server-Side Discovery)

*   **핵심 정의:** 클라이언트는 서비스의 논리적 이름으로 요청을 보내고, 중간에 위치한 라우터/로드 밸런서가 서비스 레지스트리에서 실제 서비스 인스턴스의 네트워크 위치를 조회하여 요청을 포워딩하는 방식입니다.
*   **동작 원리:**
    *   서비스 인스턴스는 시작 시 자신의 네트워크 위치를 서비스 레지스트리에 등록합니다.
    *   클라이언트는 서비스의 논리적 이름(예: `user-service`)으로 라우터/로드 밸런서에 요청을 보냅니다.
    *   라우터/로드 밸런서는 서비스 레지스트리에 질의하여 `user-service`의 가용 인스턴스 목록을 가져옵니다.
    *   라우터/로드 밸런서는 로드 밸런싱 알고리즘을 사용하여 인스턴스 중 하나를 선택하고 해당 인스턴스로 요청을 프록시합니다.
*   **장점:**
    *   클라이언트는 서비스 디스커버리 로직을 알 필요가 없어 구현이 간단합니다.
    *   다양한 기술 스택의 클라이언트를 쉽게 지원할 수 있습니다.
    *   로드 밸런싱 로직을 중앙에서 관리하고 변경하기 용이합니다.
    *   API 게이트웨이와 같은 추가적인 기능(인증, 인가, 로깅 등)을 통합하기 용이합니다.
*   **단점:**
    *   라우터/로드 밸런서라는 추가적인 인프라 구성 요소가 필요하며, 이는 단일 장애 지점이 될 수 있습니다.
    *   라우터/로드 밸런서가 병목 지점이 될 수 있으므로, 고가용성 및 확장성을 고려해야 합니다.
    *   클라이언트 요청이 라우터/로드 밸런서를 거치므로 네트워크 홉이 추가됩니다.
*   **적합한 상황:**
    *   클라이언트의 수가 많고, 다양한 기술 스택으로 구성되어 있는 경우.
    *   클라이언트가 서비스 디스커버리 로직에 대해 알 필요가 없도록 추상화하고 싶은 경우.
    *   API 게이트웨이와 같은 중앙 집중식 진입점을 통해 추가적인 기능을 제공해야 하는 경우.
    *   로드 밸런싱 및 라우팅 정책을 중앙에서 관리하고 싶은 경우.

### 4. 실무에서 주의할 점

*   **서비스 레지스트리의 고가용성:** 서비스 디스커버리의 핵심인 서비스 레지스트리가 단일 장애 지점이 되지 않도록 클러스터링 및 복제 구성을 통해 고가용성을 확보해야 합니다.
*   **캐싱:** 서비스 레지스트리 조회는 네트워크 오버헤드를 발생시키므로, 클라이언트나 라우터/로드 밸런서 측에서 서비스 인스턴스 목록을 적절히 캐싱하여 성능을 최적화해야 합니다.
*   **헬스 체크:** 서비스 레지스트리에 등록된 인스턴스가 실제로 정상 동작하는지 주기적으로 헬스 체크를 수행하여, 비정상 인스턴스로 요청이 전달되지 않도록 해야 합니다.
*   **TTL(Time-To-Live):** 서비스 인스턴스가 비정상 종료되거나 네트워크에서 사라졌을 때, 서비스 레지스트리에서 해당 인스턴스 정보를 적절한 시간 내에 제거할 수 있도록 TTL 설정을 고려해야 합니다.
*   **DNS 기반 디스커버리:** 일부 환경에서는 DNS를 서비스 레지스트리처럼 활용하여 서비스 디스커버리를 구현하기도 합니다. 이는 DNS의 캐싱 메커니즘을 활용할 수 있다는 장점이 있지만, 동적인 서비스 변경에 대한 즉각적인 반영이 어려울 수 있습니다.

## 🤖 AI 피드백

(AI 자동 작성 - 검토 필요)
